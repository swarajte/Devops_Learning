apiVersion: apps/v1
kind: Deployment

metadata:
  name: nginx-no-toleration

spec:
  replicas: 16

  selector:
    matchLabels:
      app: nginx

  template:
    metadata:
      labels:
        app: nginx

    spec:
      containers:
      - name: nginx
        image: nginx
